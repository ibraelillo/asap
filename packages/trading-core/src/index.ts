/**
 * `@repo/trading-core` exposes the innermost onion layer:
 * pure contracts, schemas, and domain events with no infrastructure coupling.
 */
export * from "./contracts";
export * from "./events";
