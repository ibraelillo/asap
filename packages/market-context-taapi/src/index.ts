/**
 * `@repo/market-context-taapi` adapts the async TAAPI client into a prepared,
 * synchronous `IndicatorProvider` so the pure `market-context` layer can stay
 * unchanged.
 */
export * from "./types";
export * from "./provider";
